---
// src/layouts/BaseLayout.astro
import "../styles/base.css";

const currentPath = new URL(Astro.request.url).pathname;
---
<!doctype html>
<html lang="en" class="h-full dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>LearnLite</title>
  </head>
  <body class="min-h-screen bg-gray-100 dark:bg-gray-900 font-sans">
    <!-- Header -->
    <header class="sticky top-0 z-50 bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 px-6 py-3 flex items-center justify-between">
      <h1 class="text-xl font-bold text-gray-900 dark:text-white">LearnLite</h1>
    </header>

    <div class="flex min-h-[calc(100vh-57px)]">
      <aside class="w-64 bg-white dark:bg-gray-800 border-r border-gray-200 dark:border-gray-700 p-6 flex flex-col">
        <nav class="flex flex-col gap-3 text-gray-700 dark:text-gray-300">
          <a 
            href="/" 
            class={currentPath === "/" ? "text-blue-600 dark:text-blue-400 font-semibold" : "hover:text-gray-900 dark:hover:text-white"}
          >
            Dashboard
          </a>
          <a 
            href="/schedule" 
            class={currentPath === "/schedule" ? "text-blue-600 dark:text-blue-400 font-semibold" : "hover:text-gray-900 dark:hover:text-white"}
          >
            Schedule
          </a>
          <a 
            href="/resources" 
            class={currentPath === "/resources" ? "text-blue-600 dark:text-blue-400 font-semibold" : "hover:text-gray-900 dark:hover:text-white"}
          >
            Resources
          </a>
        </nav>

      </aside>

      <main class="flex-1 p-8">
        <slot />
      </main>
    </div>
  </body>
</html>
